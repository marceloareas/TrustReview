import { Typography } from "@mui/material";

const AppTitle = () => {
  return (
    <Typography
      variant="h3"
      sx={{
        top: "8px",
        left: "16px",
        fontWeight: "bold",
        fontFamily: '"Marko One", serif',
      }}
    >
      TrustReview
    </Typography>
  );
};

export default AppTitle;
